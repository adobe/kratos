apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: default
  name: admin-pipeline
rules:
- apiGroups: ["", "apps", "v1", "*"] # "" indicates the core API group
  resources: ["*"]
  verbs: ["*"]
